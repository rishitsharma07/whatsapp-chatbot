services:
  - type: web
    name: whatsapp-chatbot
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/whatsapp-chatbot-0.0.1-SNAPSHOT.jar
    envVars:
      - key: JAVA_VERSION
        value: 17